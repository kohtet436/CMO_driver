enum PageIndex {
  HOME,
  PROFILE,
  INFORMATION,
  LEAVE,
  SETTING,
  LOG_OUT,
}

enum LeaveType {
  DRIVER_LEAVE,
  VEHICLE_LEAVE,
}

enum LeaveDayCount {
  ONE_DAY,
  MORE_THAN_ONE_DAY,
}

enum EmployeeAttendance {
  COMING,
  LEAVE,
  PRESENT,
  ABSENT,
  LATE,
}

enum DailyRouteType {
  MORNING,
  EVENING,
}
